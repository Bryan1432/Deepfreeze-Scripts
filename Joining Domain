$domain = "romaisd.com"

$user = "bhernandez"

$password = "Tech1432" | ConvertTo-SecureString -asPlainText -Force

$username = "$domain\$user"

$user2 = "InteractiveFlatPanel"

$pass2 = "teach"

$lcred = New-object –TypeName System.Management.Automation.PSCredential -ArgumentList ($user2, $pass2)

$credential = New-Object System.Management.Automation.PSCredential($user,$password)

Add-Computer -DomainName $domain -Credential $credential -LocalCredential $lcred -verbose -Restart -Force
